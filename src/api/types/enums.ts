export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum Action {
  CREATE = "create",
  READ = "read",
  UPDATE = "update",
  DELETE = "delete",
}

export enum Doctype {
  USER = "user",
  PERMISSION = "permission",
}
